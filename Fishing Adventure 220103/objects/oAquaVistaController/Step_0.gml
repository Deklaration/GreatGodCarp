
if (live_call()) return live_result;
if keyboard_check_pressed(vk_shift) 
{
	for (var i = 0; i < array_length(tankGuide); ++i) {
  show_debug_message(tankGuide[i])
}
show_debug_message(tankGuide)
show_debug_message(global.showAquaVistaLight)
active = true
}
