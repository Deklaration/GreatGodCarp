bobbingSpeed = random_range(0.005,0.05); // Adjust this value to make the motion faster or slower
bobbingAmount = random_range(1,5);  // Adjust this value to make the motion larger or smaller
bobbingTime = 0;
bobbingSpeedX = random_range(0.005,0.05);; // Adjust to make the motion faster or slower
bobbingAmountX = random_range(1,100);  // Adjust to make the motion larger or smaller
bobbingTimeX = 0;
timer = 80

goBackToTheMiddle = false;

y_original = y;
x_original = x;

image_speed = 0